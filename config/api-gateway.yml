server:
  port: 5000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:4000/eureka
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: CUSTOMER-JPA
          uri: lb://CUSTOMER-JPA
          predicates:
            - Path=/customers/**
